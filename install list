1. npm i express
2. Babel
    npm install @babel/node @babel/core --save-dev
    npm install @babel/preset-env --save-dev

    babel.config.json 파일 만들어서
     {
         "presets": ["@babel/preset-env"]
     } 넣어주기
3. nodemon (= 내용 수정시 자동 재시작)
    npm i nodemon --save-dev
